深度学习与智能体开发全流程培训手册
适用对象：零基础新入组同学
培训目标：从连接服务器开始，到独立开发一个 基于 RAG 的 AI 航空知识问答系统（Aero-Agent）
本地环境：Windows / Mac 笔记本
远程环境：Linux 工作站（宿主机）
预计总用时：约三周（每天1～2小时，可自行安排节奏）

目录
环境准备与远程连接

Docker 环境搭建与容器运行

Git 版本控制与 GitHub 协作

项目实战：搭建 Aero-Agent 航空知识助手

常用命令速查表

常见问题排查

环境准备与远程连接
1. 安装 VSCode
去 https://code.visualstudio.com/ 下载并安装。

2. 安装 Remote-SSH 扩展
点击左侧 扩展图标（四个方块）或按 Ctrl+Shift+X

搜索 Remote - SSH（Microsoft 出品），点击 安装

3. 配置 SSH 连接
SSH 就像一根加密的网线，让你用自己的笔记本安全地操作远在机房的服务器。

按 F1 或 Ctrl+Shift+P 打开命令面板

输入 Remote-SSH: Connect to Host... → 选择 添加新主机

输入连接命令，例如：ssh lxc@10.102.81.149

选择保存配置文件的路径（默认即可）

连接成功后，VSCode 左下角会出现 SSH: 10.102.81.149

💡 如果你有多台机器，可以在本地 SSH 配置文件中记录别名，方便快速连接。
例如在 ~/.ssh/config 中添加：

Host mylab
    HostName 10.102.81.149
    User lxc
以后只需 ssh mylab 即可连接。

4. 打开远程文件夹
点击左侧“资源管理器” → 打开文件夹

输入你在工作站上的项目路径，例如 /data/lxc（你的用户名）

此后所有文件编辑和终端命令都在远程工作站上执行

5. 安装远程开发常用扩展
在扩展商店安装时，注意选择 “在 SSH: xxx 中安装”，推荐安装：

Python (Microsoft)

Docker (Microsoft)

6. 终端的使用
按 Ctrl+` 打开终端，该终端直接操作远程工作站

你可以像在本地一样执行 Linux 命令

Docker 环境搭建与容器运行
1. Docker 三要素
镜像 (Image)：打包好的环境模板，包含操作系统 + Python + 各种库，是只读的。

容器 (Container)：从镜像启动的运行实例，互相隔离，用完可以随时删除。

Dockerfile：构建镜像的“配方说明书”，里面写着一行行指令。

2. 编写 Dockerfile
在项目目录下新建文件 Dockerfile（注意没有后缀名），常用指令示例：

FROM python:3.10-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY app.py .
CMD ["python", "app.py"]
3. 编写 requirements.txt
列出项目需要的 Python 包，一行一个，例如：

torch
torchvision
matplotlib
4. 构建镜像
docker build -t digit-app .
-t 给镜像起个名字，如 digit-app

末尾的 . 表示用当前目录下的文件进行构建

首次构建会下载基础镜像和依赖，耗时较长

网络慢时，可更换清华镜像源：
修改 Dockerfile 中 pip install 那一行为：

dockerfile
RUN pip install --no-cache-dir -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
5. 运行容器
常用命令：

# 前台交互运行，退出后自动删除（调试时常用）
docker run -it --rm digit-app

# 后台运行，并将宿主机 /data/output 映射到容器 /app/output（持久化输出文件）
docker run -d -v /data/output:/app/output digit-app
常用参数说明：

-it：交互模式，可实时看到输出

--rm：容器停止后自动删除

-d：后台运行

-v 宿主机路径:容器路径：目录挂载，让容器里的文件保存到宿主机

--gpus all：使用全部 GPU（如果有）

--name 容器名称：给容器起一个有意义的名字

6. 常用 Docker 命令
命令	说明
docker ps	查看正在运行的容器
docker ps -a	查看所有容器（包括已停止的）
docker images	查看本地镜像列表
docker stop 容器名	停止容器
docker rm 容器名	删除容器
docker rmi 镜像名	删除镜像
docker logs 容器名	查看容器日志
docker exec -it 容器名
/bin/bash	进入运行中的容器内部
docker cp 宿主机文件
容器名:路径	拷贝文件进/出容器
docker system prune	清理无用资源（慎用）

Git 版本控制与 GitHub 协作
1. 配置 Git 身份
git config --global user.name "你的姓名"
git config --global user.email "你的邮箱"
2. 在 GitHub 上创建仓库
登录 GitHub，点击右上角 + → New repository

填写仓库名（如 aero-agent-project），选择 Public 或 Private（

不要勾选“Initialize this repository with a README”

创建后，记下仓库的 SSH 地址，例如：git@github.com:用户名/仓库名.git

3. 配置 SSH 免密推送
在工作站终端执行：

ssh-keygen -t ed25519 -C "你的邮箱"   # 一路回车
cat ~/.ssh/id_ed25519.pub            # 复制输出的公钥
在 GitHub 页面：右上角头像 → Settings → SSH and GPG keys → New SSH key，粘贴公钥并保存。

4. 初始化本地仓库并推送
cd 你的项目目录
git init
git add .
git commit -m "第一次提交"
git remote add origin git@github.com:用户名/仓库名.git
git branch -M main
git push -u origin main
5. 日常使用流程
git add .
git commit -m "描述修改了哪些内容"
git push
6. 常用 Git 命令
命令	说明
git status	查看文件状态
git log --oneline	查看提交历史
git pull	从远程拉取最新代码
git clone 仓库地址	克隆整个仓库到本地
git rm --cached 文件名	从跟踪中移除，但保留本地文件
git checkout -b 分支名	创建并切换到新分支
git merge 分支名	合并指定分支到当前分支
git tag -a 标签名 -m "说明"	创建一个版本标签
git push origin 标签名	推送标签到远程


项目实战：搭建 Aero-Agent 航空知识助手
本部分将带你从零开始，构建一个基于 RAG（检索增强生成）的航空气动知识问答系统。
整个项目分为以下几个阶段，你可以按顺序一步步完成。

阶段一：项目初始化与基础工具
1.1 创建项目骨架
cd /data/lxc
mkdir aero-agent-project && cd aero-agent-project
git init
git config user.email "你的邮箱"
git config user.name "你的名字"
echo "# AeroAgent: 专业气动设计 AI 助手" > README.md
git add README.md
git commit -m "chore: initial commit - project foundation"

在 GitHub 上创建同名仓库，并关联推送：

git remote add origin git@github.com:用户名/aero-agent-project.git
git pull origin main --rebase
git push -u origin main
1.2 建立目录结构
mkdir -p backend/app/utils
mkdir -p backend/app/core
mkdir -p backend/app/services
mkdir -p frontend
mkdir -p data/raw_docs
mkdir -p .github/workflows
1.3 配置 Python 虚拟环境
虚拟环境可以为每个项目创建独立的 Python 运行空间，避免不同项目之间的库版本冲突。

python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
pip install black flake8 pre-commit
1.4 编写第一个工具：文件处理器 (FileProcessor)
创建 backend/app/utils/file_util.py，实现基础的文本文件读写功能。
（此处先提供简易版本，后续会不断升级）

import os
import json

class FileProcessor:
    def __init__(self, folder_path):
        self.folder_path = folder_path

    def read_text(self, file_name):
        full_path = os.path.join(self.folder_path, file_name)
        with open(full_path, 'r', encoding='utf-8') as f:
            return f.read()

    def save_text(self, file_name, content):
        os.makedirs(self.folder_path, exist_ok=True)
        full_path = os.path.join(self.folder_path, file_name)
        with open(full_path, 'w', encoding='utf-8') as f:
            f.write(content)
        print(f"✅ 文件已成功保存至: {full_path}")

if __name__ == "__main__":
    processor = FileProcessor("data/raw_docs")
    sample_data = {"project": "AeroAgent", "version": "0.1"}
    processor.save_text("config.json", json.dumps(sample_data, indent=2))
    print(processor.read_text("config.json"))
运行测试：

export PYTHONPATH=$PYTHONPATH:$(pwd)/backend
python backend/app/utils/file_util.py
看到 ✅ 文件已成功保存至: data/raw_docs/config.json 即表示成功。

1.5 忽略虚拟环境并提交
echo ".venv/" >> .gitignore
git add .
git commit -m "feat: implement file processor and project setup"
git push origin main
阶段二：HTTP 工具与日志系统
2.1 HTTP 请求工具
安装依赖：

pip install requests
创建 backend/app/utils/http_util.py，编写一个带异常处理的 HTTP 客户端：

import requests
from app.core.logger import logger

def fetch_json(url):
    try:
        resp = requests.get(url, timeout=10)
        resp.raise_for_status()
        return resp.json()
    except Exception as e:
        logger.error(f"HTTP 请求失败: {e}")
        return None

if __name__ == "__main__":
    data = fetch_json("https://jsonplaceholder.typicode.com/todos/1")
    print(data)
运行：

python backend/app/utils/http_util.py
终端打印出 {'userId': 1, 'id': 1, 'title': '...', 'completed': False} 表示成功。

2.2 日志模块
在 backend/app/core/ 下创建 logger.py，封装统一的日志输出：

import logging
import os

def setup_logger(name=__name__, log_file=None, level=logging.INFO):
    logger = logging.getLogger(name)
    logger.setLevel(level)
    formatter = logging.Formatter('%(asctime)s - %(name)s - %(levelname)s - %(message)s')
    # 控制台输出
    ch = logging.StreamHandler()
    ch.setFormatter(formatter)
    logger.addHandler(ch)
    # 文件输出
    if log_file:
        os.makedirs(os.path.dirname(log_file), exist_ok=True)
        fh = logging.FileHandler(log_file)
        fh.setFormatter(formatter)
        logger.addHandler(fh)
    return logger

logger = setup_logger("aero-agent", log_file="data/logs/app.log")
修改 http_util.py 中的 from app.core.logger import logger，让请求过程开始记录日志。

运行测试：

python backend/app/utils/http_util.py
检查 data/logs/ 目录下是否生成了 .log 文件。

提交：

git add .
git commit -m "feat: add http client and logging system"
git push origin main

阶段三：文件批量处理与分支管理
3.1 升级文件处理器
修改 file_util.py，增加 batch_read 方法，使其能一次性读取目录下多种格式的文件。

3.2 代码规范与 Git 分支
创建 dev 分支：git checkout -b dev

运行代码检查：flake8 backend/app/

在 VSCode 中通过 Alt+Shift+F（或保存时自动）格式化代码，修复报错

提交并在主分支合并：

git add .
git commit -m "style: fix linting issues"
git checkout main
git merge dev
git push origin main

阶段四：自动化测试与版本标签
4.1 配置 GitHub Actions
创建 .github/workflows/test.yml：

name: Python Lint
on: [push]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: pip install flake8
      - name: Lint
        run: flake8 backend/app/
提交并推送，之后去 GitHub 仓库的 Actions 选项卡查看运行结果。

4.2 打上版本标签
git tag -a v0.1 -m "Project Foundation Complete"
git push origin v0.1

阶段五：PDF 解析引擎升级（Docling）
5.1 安装 Docling
pip install docling
5.2 改造 file_util.py
用 Docling 的 DocumentConverter 替换原有的简单文本读取，新增 _read_pdf_with_docling 方法，将 PDF 转化为结构化的 Markdown。
（具体代码较长，此处略，可参考项目实际文件）

运行测试：放入一个 PDF 到 data/raw_docs/，运行 python backend/app/utils/file_util.py，观察日志和输出。

提交：

git add .
git commit -m "feat: upgrade PDF engine to Docling for semantic parsing"
git push origin main

阶段六：文本智能切分与向量化
6.1 文本切分
安装 langchain-text-splitters，创建 backend/app/utils/text_util.py，实现 TextProcessor 类，使用 RecursiveCharacterTextSplitter 按语义将长文档切成小块，并保留一定的重叠（overlap）以避免信息断裂。

pip install langchain-text-splitters
6.2 文本向量化 (Embedding)
安装 sentence-transformers，创建 backend/app/utils/embedding_util.py，使用 BAAI/bge-m3 模型将切好的文本块转换为高维向量。

pip install sentence-transformers
测试：比较“机翼在高速飞行下的压力分布”与“翼面在超音速状态的受压情况”的向量相似度，应远高于与“如何在北京买到好吃的烤鸭”的相似度。

提交：

git add .
git commit -m "feat: implement text splitter and embedding manager"
git push origin main

阶段七：向量数据库 Milvus
7.1 安装与初始化
pip install pymilvus
创建 backend/app/utils/vector_db_util.py，使用 Milvus Lite 在本地 data/ 目录下创建一个 .db 文件。定义数据集合的 Schema，向量维度需与 BGE-M3 的输出维度（1024）一致。

7.2 数据入库
将前面生成的文本块向量连同原始内容和元数据（如来源文件、章节路径）一起插入 Milvus。

7.3 检索测试
执行相似度搜索，验证能否根据查询向量召回最相关的文本块。

提交：

git add .
git commit -m "feat: implement vector database using Milvus Lite"
git push origin main

阶段八：RAG 服务核心
8.1 创建 RAG 服务
在 backend/app/services/rag_service.py 中创建 RAGService 类，串联向量检索和 Prompt 构造。
用户提问后，流程为：
提问 → 向量化 → Milvus 检索 → 组装参考资料 → 生成 Prompt。

测试时先不接 LLM，而是将生成的 Prompt 打印出来，检查参考资料是否正确。

提交：

git add .
git commit -m "feat: integrate RAG service with retrieval and prompt construction"
git push origin main

阶段九：接入大语言模型 (LLM)
9.1 安装依赖与配置密钥
pip install openai python-dotenv
在项目根目录创建 .env 文件（务必加入 .gitignore，不要提交到公开仓库）：

DEEPSEEK_API_KEY=你的真实api密钥
DEEPSEEK_BASE_URL=https://api.deepseek.com
9.2 升级 RAG 服务
修改 rag_service.py，引入 OpenAI 客户端（兼容 DeepSeek 接口），将组装好的 Prompt 发送给 LLM，获得最终答案。

运行测试：

python backend/app/services/rag_service.py
问一个与已入库文档相关的问题，观察 AI 是否依据资料给出严谨回答。

提交：

git add .
git commit -m "feat: add LLM generation to RAG service"
git push origin main

阶段十：搭建 Web 界面（Streamlit）
10.1 安装 Streamlit
pip install streamlit
10.2 编写前端界面
创建 frontend/app.py：

import streamlit as st
from app.services.rag_service import RAGService

st.set_page_config(page_title="Aero-Agent", page_icon="✈️")
st.title("✈️ Aero-Agent 航空气动知识助手")

@st.cache_resource
def load_rag():
    return RAGService()

rag = load_rag()

if "messages" not in st.session_state:
    st.session_state.messages = []

for msg in st.session_state.messages:
    with st.chat_message(msg["role"]):
        st.markdown(msg["content"])

if prompt := st.chat_input("请输入你的问题..."):
    st.session_state.messages.append({"role": "user", "content": prompt})
    with st.chat_message("user"):
        st.markdown(prompt)
    with st.chat_message("assistant"):
        with st.spinner("思考中..."):
            answer, _ = rag.answer_question(prompt)
            st.markdown(answer)
    st.session_state.messages.append({"role": "assistant", "content": answer})
10.3 启动应用
export PYTHONPATH=$PYTHONPATH:$(pwd)/backend
streamlit run frontend/app.py --server.port 8501
在浏览器打开 http://localhost:8501，即可开始与你的航空知识助手对话。

提交：

git add .
git commit -m "feat: add Streamlit web interface"
git push origin main

阶段十一：批量文档自动化入库
创建 backend/ingest_docs.py，自动扫描 data/raw 目录下的所有 PDF，调用 FileProcessor → TextProcessor → EmbeddingManager → VectorDBManager 完成全流程入库。

运行：

python backend/ingest_docs.py
之后在 Streamlit 中提问新文档里的内容，验证知识库是否已更新。

提交：

git add .
git commit -m "feat: implement batch document ingestion pipeline"
git push origin main

阶段十二：专业推理大脑与优化
创建 backend/app/services/query_service.py，精心设计 System Prompt，要求 LLM 严格依据参考资料，并提供可溯源的答案。

编写 backend/main_agent.py 作为终端测试入口。

可根据需要引入 Rerank 机制，提高检索精度。

常用命令速查表

SSH
命令	说明
ssh 用户@主机	连接远程服务器
ssh-keygen -t ed25519	生成 SSH 密钥对
cat ~/.ssh/id_ed25519.pub	查看公钥（用于添加至 GitHub）

Linux 基础
命令	说明
pwd	显示当前目录
ls	列出文件
cd 目录	切换目录
mkdir 目录名	创建文件夹
rm 文件名	删除文件
source .venv/bin/activate	激活 Python 虚拟环境
export PYTHONPATH=...	设置 Python 模块搜索路径

Docker
命令	说明
docker build -t 名字 .	构建镜像
docker run -it --rm 名字	前台运行容器
docker ps	查看运行中的容器
docker images	查看本地镜像
docker stop 容器名	停止容器
docker rm 容器名	删除容器
docker exec -it 容器名 bash	进入容器内部
docker logs -f 容器名	实时查看日志

Git
命令	说明
git init	初始化仓库
git add .	暂存所有改动
git commit -m "..."	提交
git remote add origin 地址	关联远程仓库
git push -u origin main	推送到远程
git pull	拉取远程更新
git status	查看状态
git checkout -b 分支名	创建并切换分支
git merge 分支名	合并分支
git tag -a 标签名 -m "说明"	创建标签
git push origin 标签名	推送标签

常见问题排查
症状	可能原因	解决方法
VSCode 连不上工作站	SSH 配置错误 / 网络不通	检查 IP 和用户名，尝试 ping 工作站 IP
docker build 下载慢或超时	默认 PyPI 源在国外	使用清华镜像 -i https://pypi.tuna.tsinghua.edu.cn/simple
docker run 报 ModuleNotFoundError	依赖没写全	在 requirements.txt 中补上缺失的包并重新构建
Git 推送报 Permission denied	SSH 公钥未添加	按本文添加公钥到 GitHub
git push 报 failed to push	远程有本地没有的提交	先 git pull origin main --rebase 再推送
容器中产生的文件找不到	未挂载目录	使用 -v 参数挂载宿主机目录
Milvus 检索结果为空	未入库或维度不一致	检查是否先运行了 ingest_docs.py，且向量维度与数据库定义一致
LLM 回答“资料不足”	文档确实未覆盖该问题	确认 PDF 内容是否包含相关信息，或增加检索数量

本文档由李信淳整理，欢迎新同学随时补充问题和技巧。
完成整个流程后，你将具备独立开发 深度学习应用 + RAG 智能体 的能力。