
# 深度学习开发环境培训手册

> 适用对象：新入组同学（零基础友好）  
> 基础环境：本地 Windows/Mac 笔记本 + 远程 Linux 工作站（宿主机）  
> 培训目标：掌握 VSCode 远程开发、Docker 环境打包、Git 版本控制

---

## 目录
1. [整体工作流程](#整体工作流程)
2. [VSCode + SSH 远程连接](#vscode--ssh-远程连接)
3. [Docker 环境搭建与容器运行](#docker-环境搭建与容器运行)
4. [Git 版本控制与 GitHub 推送](#git-版本控制与-github-推送)
5. [手写数字识别完整示例](#手写数字识别完整示例项目流程)
6. [常用命令速查表](#常用命令速查表)
7. [常见问题排查](#常见问题排查)

---

## 整体工作流程

你在自己笔记本上用 **VSCode** 写代码，通过 **SSH** 连到实验室的 Linux 工作站，所有代码和运行都在工作站上。  
代码写好后，用 **Docker** 把运行环境（Python 版本、依赖库等）打包成镜像，保证任何机器上都能跑。  
最后用 **Git** 把源代码推送到 GitHub，方便协作和版本管理。
[你的笔记本] --SSH--> [Linux 工作站]
|
[写代码 / 构建 Docker 镜像 / 训练模型]
|
[Git push 到 GitHub]

---

## VSCode + SSH 远程连接

1. 安装 VSCode
从 [https://code.visualstudio.com/](https://code.visualstudio.com/) 下载并安装。

2. 安装 Remote-SSH 扩展
- 点击左侧活动栏的**扩展图标**（四个方块）或按 `Ctrl+Shift+X`
- 搜索 **Remote - SSH**（发布者 Microsoft），点击 **安装**

3. 配置 SSH 连接
- 按 `F1` 或 `Ctrl+Shift+P` 打开命令面板
- 输入 `Remote-SSH: Connect to Host...` → 选择 **Add New SSH Host...**
- 输入连接命令：
  ```bash
  ssh 用户名@工作站IP地址
例如：ssh lxc@10.102.81.149

选择 SSH 配置文件保存位置（默认第一个即可）

连接成功后，左下角会显示 SSH: IP地址

4. 打开远程文件夹
点击左侧“资源管理器” → 打开文件夹

输入远程路径，例如 /data/你的用户名

之后所有文件操作均在远程工作站上进行

5. 安装远程环境下的扩展（重要）
连接远程后，安装的扩展需要选择 在 SSH: xxx 中安装，推荐安装：

Python (Microsoft)

Docker (Microsoft)

6. 终端使用
<Ctrl+`> 打开终端，该终端直接操作远程工作站

可以像本地一样执行 Linux 命令

Docker 环境搭建与容器运行
1. Docker 概念速览
镜像 (Image)：只读的“模具”，包含运行环境（系统 + 依赖）

容器 (Container)：从镜像启动的“运行实例”，互相隔离

Dockerfile：定义如何构建镜像的文本文件（说明书）

2. 编写 Dockerfile
在项目目录下新建文件 Dockerfile（无后缀），常用指令：

dockerfile
基础镜像
FROM python:3.10-slim

工作目录
WORKDIR /app

复制依赖清单并安装
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

复制代码
COPY app.py .

容器启动时执行的命令
CMD ["python", "app.py"]
3. 编写 requirements.txt
列出项目所需的 Python 包，一行一个：

torch
torchvision
matplotlib
4. 构建镜像
docker build -t 镜像名:标签 .
-t 指定镜像名称，例如 digit-app:latest

末尾的 . 表示构建上下文为当前目录

首次构建会下载基础镜像和依赖，耗时较长

网络慢时使用清华镜像源：在 Dockerfile 的 RUN pip install 行改为：

dockerfile
RUN pip install --no-cache-dir -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
5. 运行容器
docker run [选项] 镜像名
常用选项：

-it：交互模式，可实时看到输出

--rm：容器退出后自动删除（调试时推荐）

-v 宿主机路径:容器路径：挂载目录，实现数据持久化（如保存模型）

-d：后台运行

示例：

前台运行，结束后自动删除
docker run -it --rm digit-app

后台运行并将宿主机 /data/output 映射到容器 /app/output
docker run -d -v /data/output:/app/output digit-app
6. 常用 Docker 命令
命令	说明
docker ps	查看正在运行的容器
docker ps -a	查看所有容器（包括已停止）
docker images	查看本地镜像列表
docker stop 容器名	停止容器
docker rm 容器名	删除容器
docker rmi 镜像名	删除镜像
docker logs 容器名	查看容器日志
docker exec -it 容器名
/bin/bash	进入运行中的容器
docker cp 宿主机文件
容器名:路径	拷贝文件到容器
docker system prune	清理无用资源（慎用）
Git 版本控制与 GitHub 推送

1. 配置 Git 用户信息
git config --global user.name "你的姓名"
git config --global user.email "你的邮箱"
2. 创建 GitHub 仓库
登录 GitHub，点击右上角 + → New repository

填写仓库名（如 digit-recognition），选择 Public/Private

不要勾选 "Initialize this repository with a README"

点击创建，记下 SSH 地址：git@github.com:用户名/仓库名.git

3. 配置 SSH 密钥（免密推送）
生成密钥，一路回车
ssh-keygen -t ed25519 -C "你的邮箱"

查看并复制公钥
cat ~/.ssh/id_ed25519.pub
将公钥内容添加到 GitHub：Settings → SSH and GPG keys → New SSH key

4. 初始化本地仓库
cd 你的项目目录
git init
5. 创建 .gitignore 忽略不必要文件
新建 .gitignore 文件，写入：

*.pth
output/
__pycache__/
*.pyc
.venv/
data/
6. 添加、提交、推送
添加所有更改
git add .

提交到本地仓库
git commit -m "提交说明"

关联远程仓库
git remote add origin git@github.com:用户名/仓库名.git

将分支重命名为main
git branch -M main

推送到Github
git push -u origin main
7. 后续日常使用
git add .
git commit -m "描述修改内容"
git push
8. 常用 Git 命令
命令	说明
git status	查看当前状态
git log --oneline	查看提交历史
git pull	从远程拉取更新
git clone 仓库地址	克隆远程仓库到本地
git rm --cached 文件名	从跟踪中移除（保留本地文件）
手写数字识别完整示例（项目流程）
下面用我们训练过的 MNIST 项目串联全部工具。

1. 通过 SSH 连上工作站，打开 /data/你的用户名 文件夹
2. 创建项目文件
在 VSCode 中新建：

app.py（PyTorch CNN 模型代码）

Dockerfile（环境定义）

requirements.txt（torch, torchvision, matplotlib）

.gitignore（忽略模型和数据）

3. 构建 Docker 镜像
docker build -t digit-app .
4. 训练模型（挂载输出目录）
mkdir -p output
docker run -it --rm -v $(pwd)/output:/app/output digit-app
代码中需将结果保存到 /app/output/。

5. 查看结果
模型文件：output/digit_classifier.pth

预测示例图：output/predictions.png

6. 编写预测脚本 predict.py（单张图片推理）
7. 初始化 Git 并推送
git init
git add .
git commit -m "Initial commit"
git remote add origin git@github.com:miyahane-li/digit-recognition.git
git branch -M main
git push -u origin main
常用命令速查表
SSH
命令	说明
ssh 用户@主机	连接远程服务器
ssh-keygen -t ed25519	生成 SSH 密钥对
cat ~/.ssh/id_ed25519.pub	查看公钥
Linux 基础
命令	说明
pwd	显示当前路径
ls	列出文件
cd 目录	切换目录
mkdir 目录名	创建文件夹
rm 文件名	删除文件
nano 文件名	命令行编辑文件
source .venv/bin/activate	激活 Python 虚拟环境
Docker
命令	说明
docker build -t 名字 .	构建镜像
docker run -it --rm 名字	运行容器（前台）
docker ps	查看运行容器
docker images	查看镜像
docker stop 容器名	停止容器
docker rm 容器名	删除容器
docker exec -it 容器名
bash	进入容器
docker logs -f 容器名	实时查看日志
Git
命令	说明
git init	初始化仓库
git add .	暂存所有更改
git commit -m "..."	提交
git remote add origin 地址	关联远程仓库
git push -u origin main	推送到远程
git pull	拉取远程更新
git status	查看状态
常见问题排查
症状	可能原因	解决方案
VSCode 连不上工作站	SSH 配置错误 / 网络不通	检查 IP 和用户名，ping 一下工作站
docker build 下载慢或超时	默认 PyPI 源在国外	使用清华镜像 -i
https://pypi.tuna.tsinghua.edu.cn/simple
docker run 报
ModuleNotFoundError	依赖没写全	在 requirements.txt 中补上缺失的包并重新构建
Git 推送报
Permission denied	SSH 公钥未添加	按本文添加公钥到 GitHub
git push 报
failed to push	远程有本地没有的提交	先 git pull origin main
--rebase 再推送
容器中产生的文件找不到	未挂载目录	使用 -v 参数挂载宿主机目录
本文档由李信淳整理，持续更新。
欢迎新同学补充在实践中遇到的问题和技巧。
1234567